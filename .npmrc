# vitest@5 declares vite as a peer dependency; legacy-peer-deps avoids ERESOLVE when the
# old vite (bundled by vitest@2 internally) is still in node_modules during an upgrade.
# Safe here: walrus-client has no peer deps of its own that this could mask.
legacy-peer-deps=true
